# Agent Notes

- Use this Python interpreter for the current environment:
  `C:/Users/admin/anaconda3/envs/cuda_Vit/python.exe`
- Do not fall back to the default `python` path unless the user explicitly asks.
